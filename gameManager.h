#include <stdlib.h>
#include <iostream>
#include <string>
#include <vector>

class gameManager
{
private:
    int turns;
    int noBulletsCnt;

public:
    gameManager(/* args */);
    ~gameManager();
};

gameManager::gameManager(/* args */)
{
}

gameManager::~gameManager()
{
}
